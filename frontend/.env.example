# Only needed if you deploy the frontend SEPARATELY (e.g. Vercel/Netlify)
# instead of letting the Flask backend serve it.
# Point it at your Hugging Face backend Space (no trailing slash):
VITE_API_BASE=https://marripelli-youtube-backend.hf.space
